{
  "name": "@thread-to-pdf/server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "postinstall": "node scripts/install-chrome.mjs || true",
    "install:chrome": "puppeteer browsers install chrome",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@thread-to-pdf/shared": "workspace:*",
    "fastify": "^5.2.0",
    "lru-cache": "^11.0.2",
    "pdf-lib": "^1.17.1",
    "puppeteer": "^23.11.1",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
