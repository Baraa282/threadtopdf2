# Direct API URL (optional). Leave empty on Vercel — the /api proxy is used instead.
# Example: https://thread-to-pdf-api.onrender.com
VITE_API_URL=

# Vercel serverless proxy (set in Vercel dashboard, not in this file)
# RENDER_API_URL=https://thread-to-pdf-api.onrender.com
